<Page
    x:Class="WindowHand.Views.Pages.SystemPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WindowHand.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="SystemPage"
    d:DataContext="{d:DesignInstance local:SystemPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">
    <Grid Margin="24,16,24,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock
                FontSize="22"
                FontWeight="SemiBold"
                Text="システム" />
            <TextBlock
                Margin="0,6,0,0"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                Text="ms-settings のショートカット一覧です。"
                TextWrapping="Wrap" />
            <TextBlock
                Margin="0,12,0,0"
                Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                Text="{Binding ViewModel.ErrorMessage, Mode=OneWay}"
                TextWrapping="Wrap" />
        </StackPanel>

        <ListView
            Grid.Row="1"
            Margin="0,12,0,0"
            ItemsSource="{Binding ViewModel.Shortcuts, Mode=OneWay}"
            ScrollViewer.CanContentScroll="True"
            ScrollViewer.HorizontalScrollBarVisibility="Auto"
            ScrollViewer.VerticalScrollBarVisibility="Auto"
            VirtualizingPanel.IsVirtualizing="True"
            VirtualizingPanel.VirtualizationMode="Recycling">
            <ListView.View>
                <GridView>
                    <GridViewColumn
                        Width="180"
                        DisplayMemberBinding="{Binding CategoryName}"
                        Header="カテゴリ" />
                    <GridViewColumn
                        Width="220"
                        DisplayMemberBinding="{Binding Name}"
                        Header="名前" />
                    <GridViewColumn
                        Width="360"
                        DisplayMemberBinding="{Binding Uri}"
                        Header="URI" />
                    <GridViewColumn Width="90" Header="操作">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <ui:Button
                                    Padding="10,4"
                                    Command="{Binding DataContext.ViewModel.OpenCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                    CommandParameter="{Binding}"
                                    Content="開く" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
